import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
    selected: {
        color: 'gold',
    },
    unselected: {
        color: 'white',
    },
});

export default useStyles;
